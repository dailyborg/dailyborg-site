# workers/sentinel (sentinel-engine)

Hourly maintenance: coverage per desk (index backed), scraper trigger through a service binding at most once an hour, free Unsplash hero image repair (5 per run), daily pruning of logs, visits and old history. Secret: UNSPLASH_ACCESS_KEY (optional). POST /__run_check runs a pass now.
