{
  "name": "dailyborg-ingest",
  "version": "2.0.0",
  "private": true,
  "description": "Queue consumer: writes articles with Gemini via AIML, sends the daily and weekly briefings",
  "main": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "agents": "^0.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260409.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.75.0"
  }
}
