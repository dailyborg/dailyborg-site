# scripts/

Helper material. db-history/ holds SQL that was already run on the production database during the April 2026 cleanups plus the legacy seed files. Reference only; never re-run on production.
